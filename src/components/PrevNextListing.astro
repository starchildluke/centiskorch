---
const { prevPost, nextPost, prevSlug, prevTitle, nextSlug, nextTitle } = Astro.props;
---

<div class="post-pagination">
  {prevPost && <a href={`/blog/${prevSlug}/`} class="post-pagination-item post-pagination-prev">{prevTitle}</a>}
  {nextPost && <a href={`/blog/${nextSlug}/`} class="post-pagination-item post-pagination-next">{nextTitle}</a>}
</div>