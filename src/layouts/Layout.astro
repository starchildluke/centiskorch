---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';

interface Props {
	title: string;
	description: string;
	canonicalURL: string;
	robots: string;
}

const { title, description, robots, canonicalURL = Astro.url } = Astro.props;
---

<html lang="en" id="top">
	<BaseHead title={title} description={description} canonicalURL={canonicalURL} robots={robots} />
	<body>
	<Header />
		<slot />
	</body>
</html>