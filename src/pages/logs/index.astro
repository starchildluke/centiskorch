---
import Layout from '../../layouts/Layout.astro';
import Footer from '../../components/Footer.astro';
import LeftSidebar from '../../components/LeftSidebar.astro';
import { getCollection } from 'astro:content';
const title = "Logs";
const description = "This is where I keep my logs. No, not the woooden type but that would be kinda cool.";

const posts = await getCollection('logs');
---

<Layout title={title} description={description}/>
<div class="container">
	<div class="flex-content">
		<main class="box">
			<h1 class="headings">{title}</h1>
			<p>This page features various logs and updates to the site. My favourite is the Pokémon teams log where I showcase all the teams I've used in every game I've played.</p>
			<p>I also note down site updates in the Trainer's Log, as well as my todolist for the site and the projects I'm working on.</p>
			<ul class="pokeball-icons">
				{posts.map((post) => (
					<li>
						<a href={`/logs/${post.slug}/`} class="blog-listing-title">{post.data.title}</a>
					</li>
					))
				}
			</ul>
		</main>
		<LeftSidebar />
		<Footer />
	</div>
</div>
			